app:
  host: "0.0.0.0"
  port: 5001

backend:
  url: "http://localhost:5000"
  api_key: "your-secret-api-key"

auth:
  api_key: "your-secret-api-key"

context:
  dir: "../context"

storage:
  data_dir: "../data"
